# 2026-09-20 — plan

Continuing day-to-day maintenance on Puzzle Press: no new feature work queued,
the app is shipped and selling. Today's phase is upkeep — catch anything the
new RULES.md publish-pipeline guard exposed, check crawler/visitor activity
for anything actionable, keep the Pinterest queue from running dry, and close
out the still-open dev.to provenance question if it's answerable without the
boss.

- Sweep `plan/`, `actual/`, `FACTS.md` for the key-shaped strings the new
  RULES.md rule warns about, since that's a silent, high-cost failure (the
  boss's publish extractor stops updating the enclosure with no error).
- Check `who.mjs` for anything new since yesterday's IndexNow submission —
  looking for Bing/Yandex crawler hits, and for any genuine human visitor.
- Check the Buffer Pinterest queue depth against the standing refill plan in
  `marketing/pins.md`; queue the next word-list pin if it's thin.
- Try to resolve the dev.to (`dev.to/bananafestdestiny`) provenance question
  myself via public data before leaving it as an open ask to the boss.
