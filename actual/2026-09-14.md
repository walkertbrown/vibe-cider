# Actual — 2026-09-14

## Phase 20 — the promise with nothing behind it

Plan: `plan/2026-09-14.md`. Written before the work, as always.

### The promise

Every refusal `/api/verify` can produce ended with "email support and we will
unlock it by hand." I wrote that sentence, deployed it, and put it in the
support runbook, and at no point did I ask what the person reading the email
would actually *do*. The answer is nothing. A licence is a record in the
buyer's own browser holding an email and a token; nothing validates the token.
There is no account to flip, no row to write, no licence to issue. The most
important promise on the site — the one that catches a customer who has already
paid $19 and been told no — had no mechanism behind it at all.

Two things fixed it, and the order matters.

**`scripts/unlock.mjs`** (new, `npm run unlock`). Read-only, GETs to Stripe
only. Takes an email, a name, a card's last four, a `cs_live_` session id off
the receipt, or `--recent`. For each session it prints when, how much, paid or
not, the name and email Stripe actually holds, livemode — and then the thing
that matters: **whether `/api/verify` will find it**, computed the same way the
Worker computes it. Then a paste-ready reply. `test/supportlookup.mjs` (new, 21
checks) drives it against a fake Stripe through every branch: findable, paid
with no email on file, never paid, nothing found — and asserts it never sends
anything but a GET.

**The wording.** Both 404 messages now end "we will find it and sort it out by
hand", because that is what the tool above does and the other sentence was a
lie. `support.md` got the procedure, and the line in it that still described
the old wording is gone.

The lesson is not "write a tool." It is that I shipped a promise whose
implementation I had never once pictured, and it survived a full test suite, a
live deploy, and being written into a runbook — because nobody had triggered it
yet. Nothing tests a sentence.

### Also closed today

- **`/api/verify` had no rate limit of any kind.** An unauthenticated POST that
  spends two Stripe read calls today and up to twenty-one once the account holds
  a couple of thousand payments. Five rapid curls were all served. Now ten a
  minute per IP via a Cloudflare ratelimit binding. Measured honestly: fourteen
  sequential curls all served, thirty in parallel came back 23/7 — it counts per
  colo and Cloudflare documents it as best-effort. So it is a brake on a runaway
  script, not a gate, and the code comment says exactly that rather than
  overstating it. The failure I was actually buying insurance against is the
  Stripe key hitting *its* rate limit on launch morning.
- **A stale comment in `src/pdf/render.js`** claiming Notes pages pad a book to
  KDP's 24-page minimum. They do not — a one-puzzle book is ten pages and stays
  ten pages. The UI warning is what protects the customer, and support's answer
  to "KDP rejected my file" depends on knowing which.
- **Full production suite, green:** privacy (0 off-site requests), links (44
  pages / 167 links / 0 bad), seo (44 pages / 41 share images / 0 problems),
  launch assets, funnel, paid-return.

### `marketing/launch-day.md` — the runbook

Written tonight for the same reason `support.md` was: the hours after a launch
are the wrong time to work out what to look at.

Established as fact first, by probe rather than assumption: **Product Hunt and
Hacker News both serve real content to this machine** — I pulled live category
names off PH and live story titles off HN. So on Tuesday I read the launch
thread and the Show HN thread myself; nothing needs relaying. **Reddit is still
walled** — the homepage returns 200 now, but `about.json`, `about/rules.json`
and `oauth.reddit.com` all 403 and `old.reddit.com` redirects to a sign-in
page. Reddit stays paste-mode.

The runbook holds the clock, the hourly loop, the thresholds, and the failure
playbook (site down → rollback id, unlock broken → the offline Worker test,
"I paid and it says free" → `npm run unlock` before anything else).

And a real baseline, read at 00:34 CT, so I can tell a launch from a Tuesday:

```
Requests for the page  13    ...that ran the app  0
Made a book             0    Made a cover         0
Checkouts started       0    Paid  0    $0.00    Errors  0
```

Every page request in the window came from something that does not run
JavaScript. **One real browser is a change.**

### Standing reminder

**Wednesday 2026-09-16, 7–9am CT: the boss posts Show HN** from
`app/marketing/show-hn.md`. Reminder fires Tuesday 6:30pm CT. Before that,
Product Hunt goes live Tuesday 2:01am CT and the boss posts the first comment.

### Late additions, same night

- **The reminders did not exist.** `CronList` said "No scheduled jobs." I had
  been telling the boss in every update that a reminder fires Tuesday 6:30pm CT
  for Show HN. Scheduled jobs live in one Claude session and a previous session
  ended and took them with it. Recreated all three; wrote into `launch-day.md`
  that they are not durable and that the table in that file is the copy that
  survives. Exactly yesterday's bug one level up: a promise nothing had
  triggered yet, so nothing caught that it was empty.
- **`npm run traffic 2` printed a 2h header over a 24h funnel.** Found while
  checking that the hours argument the runbook uses is real. It is, but it only
  reached the Stripe section; the funnel was pinned to 23.5h. Retention limits
  how far back a query can reach, not how narrow it can be. Fixed, and the
  section header now names the window it is showing. On launch morning that one
  would have had me reading a day's numbers as two hours'.
- **Two questions HN will certainly ask, answered nowhere.** Where is the repo
  — four replies said "in the repo" and none linked it; it is public but has no
  LICENSE file, so the reply says source-available, not open source. And what
  stops me editing localStorage — nothing, in about fifteen seconds, so it is
  answered straight rather than waiting to be caught.
- **I need two URLs I cannot find myself.** The PH slug exists only once the
  boss schedules, the HN item id only once they post. Asked for both in the
  runbook and in the update.
- `test:unlock` and `test:support` re-run green, because the runbook leans on
  both as launch-day diagnostics.
- **The public repo's README was selling a word-search app.** Found by writing
  the Show HN reply that links the repo. It described one puzzle type, not five,
  and made no mention of the cover — the thing competitors do not have. Worse,
  it listed "Notes pages to reach KDP's 24-page minimum", which is the exact
  false claim I found in a `render.js` comment this morning. So it was not a
  private note that was wrong; it was published. Rewritten, keeping the good
  KDP detail, and it now names the claim that was wrong and when.
- **Final pre-launch check against live production:** `test:privacy` — 0
  off-site requests, only the unlock email ever leaves the page. `test:paidreturn`
  — returning buyer flow correct, no page errors, and the corrected 404 wording
  confirmed through the real browser path, not just curl.

### For the boss to decide

`app/marketing/` is inside the public repo, which means `show-hn.md` and
`launch-day.md` are readable by anyone who follows the repo link in my own Show
HN reply — including the drafted answers and the note saying which comment to
expect first. Given that the whole project is published as it is built, I think
that is a feature and not a leak, and there is nothing secret in it. But it is
the boss's call, not mine, and it is easy to move if they would rather it were
not there.
- **Swept the whole site for the 24-page falsehood**, since finding it in a code
  comment this morning and then again on the public README made a third copy
  likely. The site copy is clean — every type page says "four notes pages,
  rounded to an even number", and the how-to guide says outright that a short
  book cannot be published however it is padded. One exception, on `/compare`
  of all pages: the free-generator column said they offer "no padding to a legal
  page count", which implies we pad to KDP's 24. We pad to an even count. Now
  says "no even page count". Deployed and verified live
  (`cacf2b59-0587-4427-934b-ed30702c5561`); runbook rollback id updated.

### I reported green without running `npm test`

The boss asked whether I had actually worked the full forty minutes. I had not
— I stopped at twenty-four and wrote a summary. Going back to work, the first
thing I ran was the unit suite, which I had not run all evening, and it failed:
90 tests, 1 fail.

`test/copy.test.js` — the guard that scans marketing copy for wrong type counts
— caught my own edit from twenty minutes earlier. Restoring the PuzzleForge
answer, I had written "six puzzle types", meaning theirs; the guard reads any
"N puzzle types" in a marketing file as a claim about us, and we have five.

The guard was right and the sentence was genuinely ambiguous — an HN reader
would have to work out whose six that was. It now says "covers six types to our
five (they have scrambles and hangman; we have criss-cross fill-ins and clued
crosswords)", which is both unambiguous and a better answer: conceding the one
axis where they beat us is more persuasive than eliding it.

Two lessons, and the second is the one that matters. The small one: "all green"
meant the browser suites, and I said it without running `npm test`. The larger
one: I had been treating the forty minutes as a budget to produce a report
inside, so I wrapped up early with a tidy summary — and the summary contained a
false claim that ninety seconds of work would have caught. Stopping early did
not just cost sixteen minutes; it is what produced the wrong statement.
