# Actual — 2026-09-12

## Morning check

- **YouTube Short published** on schedule, 6:48pm yesterday:
  https://www.youtube.com/shorts/uKGoCuyMiJ8
- **Pinterest still blocks the domain.** Retried pin #1 this morning, same
  "Source URL" error. Appeal is filed (boss, 09-11); nothing to do but wait.
  The YouTube-linked pin from yesterday is live.
- **Traffic, last 24h, real domain:** 72 page renders, 0 sample opens, 0
  calculator visits, 0 downloads, 0 sales. Two Stripe checkout sessions
  opened at 09:59 yesterday, both `open/unpaid`, both in the same minute —
  that is an automated run (two tabs), not a person hesitating.
- Fixed a misleading line in `npm run traffic`: it labelled Worker
  *subrequests* as "Stripe verify calls" (9,416 of them). The Worker only
  runs for `/config.js`, `/api/*` and non-existent paths, and each of those
  does one asset fetch — so that number was scanner 404s on both hosts plus
  my own test runs, not Stripe. It now says what it is. The zone funnel was
  always the honest number.

## Phase 35 — KDP margin calculator

Deployed as `3d38e60a`.

`/margin-calculator`: trim, page count, bleed on/off → PDF page size in
inches, mm and points; inside (gutter), outside, top and bottom margins;
which side the gutter sits on for odd and even pages; the live area; a
one-line "type this into Word/InDesign" setting with mirror margins; and a
to-scale drawing of two facing pages with the safe area shaded and the
gutter marked, redrawn as you type. Built on `pageGeometry` and
`marginsForPage` from the PDF engine — 3.5 kB of script, the same
functions that place every puzzle on every page.

The warning box names the two mistakes I see in rejection threads: gutter
on the same side of every page, and bleed switched on without enlarging
the page. Gutter table, software setup, links to the spine and royalty
calculators and the guide; the guide's margins section now links back.
Footer link on every page; sitemap at 0.9.

`npm run test:margin`: every trim × ten page counts across every gutter
band × bleed on/off — 120 combinations — read off the live page and
compared to the engine's numbers. All match. Links resolve, no page
errors, no overflow at 1200 or 390. 58/58 unit tests.

Three calculators now: spine, royalty, margins. Between them they answer
the three questions every KDP interior and cover thread on the internet is
asking, each with the product's own arithmetic and a link to the product.

## Phase 36 — the landing page, read cold

Deployed as `4f0aae68`. Rendered the page to text and read it top to bottom
the way the guide was read yesterday. The pitch, the tool, the "why it
holds up" cards and the terms all read fine. **The pricing section did
not — it still described a free tier that stopped existing on Thursday:**

- Free column: "Books up to 12 puzzles — long enough for KDP." There has
  been no puzzle cap since the Notes-page fix; the paragraph two lines
  above it said "all hundred puzzles if that is what you want". A buyer
  reading the column would think the free book is short, and a buyer who
  had made a 60-puzzle free book would think the page was lying. Now:
  "Full-length books — as many puzzles as you like."
- Paid column: "Unlimited puzzles per book" and "Matching cover" — both
  free already. Now the paid column says what $19 actually changes:
  no footer line, cover without the PREVIEW mark, unlimited books.
- FAQ, cover: ended "Cover generation is part of the paid unlock" one
  sentence after saying you can generate it free. Sentence removed.
- Terms, "What $19 buys": same stale list; rewritten to match.

Smaller: the margins FAQ now points at the margin calculator as well as
the spine one; "four Notes pages, which counts towards that" → "count
towards it".

This is the same class of mistake as the launch file on Thursday — the
product changed and the copy describing the price did not. The pricing
section is the one place a stranger decides whether to trust the page, so
I have added it to the Monday checklist as a re-read, not just the tests.

## Pinterest: the native error

The boss tried to create the pin by hand on pinterest.com. Under the Link
field: **"Sorry! We blocked this link because it may lead to spam."** So it
is a domain-level spam block in Pinterest itself, not Buffer's API path —
which settles yesterday's diagnosis. He is filing the appeal under "A Pin
from my website is blocked for Spam" with that exact wording. (The 09-11
note that the appeal was already filed was wrong; he had not filed it.
FACTS corrected.)

## Live suites, then the comparison page

**All fifteen live suites pass against production** after today's edits —
browser, checkout (card form mounts, nothing charged), paid-return,
privacy, perf (still 247 KB and 0.95 s to first puzzle, unchanged by a
week of additions), the three calculators, type pages, word lists, and
the rest. Not waiting for Monday for that was the right call; Monday's run
is now a confirmation rather than a discovery.

**Phase 37 — `/compare`.** Deployed as `1024e482`. I set out to write
"Puzzle Press vs Book Bolt" with their prices on the page, and could not:
bookbolt.io returns 403 to every fetch I have (plain, browser user-agent,
headless Chromium), so the only sources for their pricing are affiliate
review sites. I will not print a competitor's price from a source I cannot
check. So the page compares **four routes** — subscription suites, free
one-puzzle generators, doing it by hand, and Puzzle Press — on what you
pay, what comes out, KDP geometry, the cover, puzzle correctness, grading,
where your words go, and who each is best for. Claims about the other
routes are limited to what they say about themselves (subscriptions bill
monthly; free generators make one puzzle); claims about us link to where
they can be checked. It says plainly when the others are the better
choice (a broad low-content catalogue; a single worksheet; an illustrated
book) and has a "what Puzzle Press does not do" section, because a
comparison that only flatters is an advert and reads as one.

Linked from the footer and from a new FAQ entry ("How is this different
from Book Bolt or a free generator?" — naming it there is fine; that is
the question people ask). `#why` anchor added to the landing page so the
comparison can point at the correctness cards. All links resolve; no
overflow at 1200 or 390. Sitemap at 0.7.

**Book Bolt's price, from the source.** The boss opened bookbolt.io/pricing
in his own browser and sent the screenshot: Pro $19.99/month billed
annually, Premium $29.99/month billed annually, 3-day trial, puzzle tools
under Pro. The affiliate review sites I had declined to quote said
$9.99/$19.99 — wrong by a tier, which is exactly why I did not use them.
The comparison page now carries the real figures, dated, with the
arithmetic: $239.88 a year for the plan with puzzle tools, against $19
once. Deployed and verified live.

## Reminders and the Show HN

- `app/marketing/show-hn.md`: title (under 80 chars), URL, the post text,
  the AI-disclosure note with a recommendation to keep it, and eight
  prepared replies (why not Book Bolt; how uniqueness is guaranteed;
  bundle size; crosswords; the 0.06" source; what the human did; privacy;
  price). Wednesday 7–9am Central, the day after Product Hunt.
- Monday's reminder (`trig_01DbeQ…`, 6:30pm CT) was still telling him to
  clear the Stripe unit label, done Thursday, and called the product a
  word-search generator. Rewritten: disclosure decision, schedule the
  launch with the video first, 2am alarm for the first comment, online
  from 6:30am, and a pointer to Wednesday's HN. It reads the launch file
  at fire time, so the checklist it sends is whatever is unticked then.
- New reminder `trig_01WBt8…` fires Tuesday 6:30pm CT and emails the Show
  HN title, text and replies verbatim from the file, ready to paste.
- AlternativeTo: dropped. Book Bolt is not listed there, so the "Book Bolt
  alternatives" page the listing was meant to land on does not exist.
  Not worth the boss's time; a 512px icon was made for it and stays live
  at `/icon-512.png` for any other directory.

## Phase 38 — the Saturday pass

Not a rest day. Six items, all shipped (last deploy has the cards):

1. **Samples re-rendered** from current code — all four had been made
   before this week's fixes. Plus two new ones: sample covers for the
   sudoku and maze books, so every type can be judged whole. All six pass
   Ghostscript strict mode; linked from the type pages; in the sitemap.
2. **Whole-site link check**: `npm run test:links` walks the sitemap (42
   pages) and every internal href/src on each — 93 distinct targets, 0
   bad. Plain fetches, no browser, fifteen seconds.
3. **Spelling**: aspell on this machine is broken (missing dictionary; it
   silently passed everything — caught by feeding it a known misspelling),
   so checked visible text against the system word lists instead. Nine
   words flagged, all real (barcode, blocklist, rotationally…). Clean.
4. **Social cards**: 38 proper 1200×630 cards (`npm run cards`) for the
   type pages, compare, margin calculator, the word-list index and all 32
   themes, replacing the portrait pin that X/Facebook/Slack were cropping.
   Two iterations on the crop — first the pin's own headline showed, then
   the image spilled over the text — fixed by clipping the panel.
5. **Sudoku and maze covers** (see 1).
6. **Accessibility**: axe-core WCAG 2 A/AA on the landing page, the margin
   calculator, a type page, a word-list page, the guide and compare — zero
   violations on all six.

## Phase 39 — a fourth type: criss-cross (word fill-in)

Deployed as `ee8c2b43` and the pin/card follow-up. **Shipped**, on the
terms set in the plan: 64/64 unit tests (six new), a 24-puzzle graded book
downloaded live through the real tool (36 pages, fonts embedded, Ghostscript
strict clean) and its cover, type-page and link suites green, the browser
suite green.

**What it is.** `src/generator/crisscross.js`: words laid longest-first,
each crossing an existing word at a shared letter, with the rule that a
non-crossing cell may have no perpendicular neighbour — that is what keeps
stray two-letter runs out of the grid. Grid cropped to the shape the words
make. Then the part that matters: `countFills` solves the finished grid by
backtracking (most-constrained slot first, cap of two) and the puzzle is
kept only if exactly one fill exists. If two exist, the longest word is
printed in as a starter and it is re-checked; if still two, the layout is
discarded. Graded 8/12/16/22 words in grids up to 11/13/15/17. A theme too
thin for a level (American States at expert: 37 long words) steps down a
level with the printed grade following, rather than failing. Whole thing
runs in single-digit milliseconds a puzzle.

**Everywhere a type has to exist:** PDF page (grid, word list grouped by
length, starter note), solutions six to a page, the cover blurb and a mini
grid on the wrap, the tool (fourth option, themes shown, word-search-only
knobs hidden, preview grid, `?kind=crisscross`), the sample book and its
cover, a type page `/criss-cross-book-generator` with its own pin and
share card, the sitemap, every footer, the landing copy (hero, "why" card,
FAQ, pricing bullet, JSON-LD), the guide, compare, the margin calculator,
the launch file and the HN post — "crosswords?" now has a better answer
than "no". PH description re-counted: 252/260.

**Honest limits, said on the page:** it is not a clued crossword and does
not claim to be. Criss-cross books are their own KDP category and that is
what this is.

## Pinterest: appeal denied, pins re-pointed

The boss reports Pinterest refused the appeal. The domain stays blocked as a
pin destination. Rather than leave five finished pins as drafts, every one
now links to the YouTube video, whose description links the tool, the guide
and the calculators — one click further than I wanted, but it is reach
instead of nothing. Each description now says where the linked page lives
so a reader can type it. Queued, Central: spine tip 7:10pm tonight; guide
11:36pm; sudoku 12:11pm Sun; mazes 3:24pm Sun; the Short as a video pin
8:34pm Sun; the new criss-cross pin 9:40pm Sun. (Buffer's Pinterest text
limit is 500 characters — the fill-in pin had to be shortened.) The errored
duplicate of the live cover pin and the stale video draft are deleted; the
board has one live pin and six scheduled.

Re-appeal in a month, when the domain has age and traffic; noted in the
launch file. Pinterest is a slow channel either way.

## Loose ends from the fourth type

- `demo.gif` regenerated (`npm run demo`): 18 frames, now ends on a
  fill-in page after the maze one; landing-page caption and alt text
  updated, and the launch file's gallery note.
- Every word-list page has a second button — "…or a Halloween fill-in
  book" — using `?kind=crisscross&theme=…`, and the closing paragraph links
  the criss-cross type page. 33 pages regenerated; link and word-list
  suites green.

## Phase 40 — clued crosswords (fifth type)

Deployed as `c46bc653` (+ demo GIF). **Shipped**, on the plan's terms: 68/68
unit tests (four new), a graded 24-puzzle book and cover downloaded live
through the tool (36 pages, Ghostscript strict clean), a 50-puzzle book
rendered offline the same way, the clues read cold — all before Monday.

**The clues, which were the risk.** `src/generator/clues.js`: 1,460
definition-style clues covering all 1,660 theme words (some words sit in
several themes). Written in one pass, then checked three ways:
1. A script, then a unit test: every theme word has a clue; no clue
   contains its answer, nor any whole word of four+ letters that is part of
   the answer. The first pass had 36 of those — "Small cake in a paper
   case" for CUPCAKE, "Fish with a flat, sharp bill" for SWORDFISH — every
   one rewritten. Two duplicate keys esbuild caught (ROAST, CASTLE) were
   merged into clues that cover both senses.
2. A read of all 1,460 as a solver. That found the cross-theme problem the
   script could not: SCALE clued as a musical scale sits in the kitchen
   list too; DICE, CARDS, BASS, CABIN, COSMOS likewise; COOKIES and CANDY
   were clued for Christmas and Halloween but appear in Desserts and
   Easter. Nine rewritten to fit every theme they are in. Also "Under the
   bonnet" (an American reader says hood) and "Added white" for MILK, which
   was just too clever.
3. Facts: the planets, the state nicknames, the dinosaur periods and the
   like were checked as I read. "Farthest planet" for NEPTUNE is right
   since Pluto was demoted; PLUTO's own clue says so.

**The generator** is the criss-cross layout (factored into `buildGrid`)
plus conventional numbering — one number where an answer starts across or
down — and Across/Down lists with lengths in brackets. Long-worded themes
(Jobs, States) may not lay at a level's grid size; the layout gets one size
up before stepping down a level, and the printed grade follows. A pasted
list takes `word — clue` lines (em dash, colon, or spaced hyphen, so
`forget-me-not — …` keeps its hyphens); words without a clue are named in
a warning and left out, never given an invented one — the live test pastes
"zorblat" and checks it is reported. Built-in clues are fetched lazily the
first time a crossword is picked (51 kB chunk), so the landing page's
first load is unchanged: 251 kB, 0.96 s to first puzzle.

**Everywhere a type has to exist:** PDF page (numbered grid sized to
whatever the two clue columns leave — a 22-clue expert grid still fits a
6×9 page), solutions six to a page, cover blurb and mini grid, the tool
(fifth option, placeholder text showing the `word — clue` form, preview
with numbers and clue columns), sample book and cover, pin, share card,
type page `/crossword-book-generator`, sitemap, footers, landing copy and
a new "why" card, FAQ, JSON-LD, guide, compare, margin calculator, the
launch file (description re-counted: 254/260) and the HN post, whose
"Crosswords?" reply now says yes and what kind. Demo GIF: 21 frames, ends
on a crossword page.

**Said plainly on the page:** these are themed crosswords — the open
interlocking grids KDP crossword books use — not dense newspaper grids.
The compare page and the HN reply say the same.
