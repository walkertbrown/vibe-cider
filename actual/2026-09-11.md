# Actual — 2026-09-11

(Phases 22 and 23 from earlier today are logged at the end of `actual/2026-09-10.md`, where the session was still running.)

## Phase 24 — the live checkout, walked by a browser

Deployed as `dd0a0bae`.

The payment-link redirect fix was verified by reading Stripe's API back. It had never been exercised by a browser following the Buy link the way a buyer does. So: real browser, live site, click Buy from the pricing block, follow the link into Stripe, and stop at the Pay button with nothing charged.

Correct all the way: the live link (not a test one), "Puzzle Press Unlimited", **$19.00**, no sandbox badge, no "per unlimited" — the unit label is confirmed cleared on the live product — and the card form mounts. Kept as `npm run test:livecheckout`. Nothing further can be verified without spending real money.

**Then a case that walk made me think about: email casing.** A buyer who types `John@Gmail.com` at checkout and `john@gmail.com` at unlock. The worker lowercased the unlock email and asked Stripe for an exact match, and Stripe's filter is exact — so that buyer would have been told their payment did not exist. The verify route now tries the email as typed and lowercased, and if neither matches, scans recent completed sessions and compares case-insensitively before it says no. Both the paid-return and privacy suites still pass: the request carries the email and nothing else.

Both repos pushed.

## Phase 25 — actually looking at the phone

Deployed as `a36238fa`. I had measured load time on a phone and checked for horizontal overflow. I had never looked at the page on one. Screenshots of an iPhone 13 viewport, top to bottom:

- **The first screen was seven links.** Pricing, two calculators, four samples — a full screen of navigation before the headline said what the product is. On a phone the header now shows the brand, the tagline and Pricing; the samples and calculators are already in the hero and footer, and the two samples that were not in the hero now are.
- **Checkboxes were 13 pixels.** Thirty-four tap targets under 32px. Checkboxes are 20px, every row is at least 44px tall on a phone, and buttons have a 44px minimum.
- **Select labels truncated** — "6" × 9" (most cc", "Black ink — usu". Shortened everywhere; they read better on desktop too.

And while the file was open: the **Open Graph and Twitter titles still said "Word Search Book Generator"** — that is what a shared link previews as on every platform, and it had been wrong since sudoku shipped. Fixed.

Verified: e2e and defaults suites pass; still 246 KB and under a second to first puzzle on a throttled phone.

## Phase 26 — the nearest thing to a KDP upload I can run

Not deployed — nothing changed in the product. This is verification.

I cannot upload to KDP. What I can do is run the checks its validator runs: interpret every PDF from the first byte to the last with Ghostscript in stop-on-error mode, and confirm every font is embedded with `pdffonts`. Twelve files — every puzzle type, free and paid, bleed on, large trim, and a cover for each — all interpret cleanly with zero warnings, every font embedded, every page size exactly what KDP expects (432×648 for 6×9, 441×666 with bleed, 612×792 letter, and the covers at back + spine + front + bleed).

Kept as `npm run test:pdfcheck`. One honest note: my first pass reported "qpdf-issues=0" for every file, which looked like a pass and was actually `qpdf` not being installed — grep counted zero matches in "command not found". I noticed because a column of perfect zeros with no tool behind it is suspicious, and swapped in Ghostscript, which is present and did the work. A green result is only worth something if the thing producing it exists.
