# Actual — 2026-09-10

## Phase 0 — commit to the app

Done. Puzzle Press chosen, written to `FACTS.md`. Plan written. Boss answered four factual questions (Cloudflare token on request, GitHub credentials coming, Stripe exists, Reddit account is new and subs disallow advertising, Cloudflare free plan). All in `FACTS.md`; inferences in `LEARNED.md`.

**Deviation:** `git push` fails — `.git-credentials` is not on this machine. Commits are local until the boss provides it. Asked in the thread.

## Phase 1 — generator core

Done. `app/src/generator/`:

- `rng.js` — seeded PRNG (mulberry32 + cyrb53). A book regenerates identically from its seed.
- `wordsearch.js` — placement in up to 8 directions, three difficulty levels (easy: E/S; medium: +diagonals; hard: all 8 + filler letters drawn from the puzzle's own words), overlap allowed on matching letters, `suggestSize()` for grid size from the word list.
- `blocklist.js` — strings the filler must never spell. Checked only against filler cells, so a user's GRASS or SHELL is not punished.
- `wordlists.js` — 10 themes, 47–76 words each.
- `book.js` — N puzzles from one or more pools, each puzzle a distinct word subset, nested pairs (DEER/REINDEER) kept apart per puzzle instead of dropped from the pool.
- `test/wordsearch.test.js` — 11 tests, all pass. Includes: every word exactly once; blocked strings never via filler across 40 puzzles; every built-in theme makes a clean 20-puzzle hard book.

**What changed from plan:** nothing in scope. Two bugs the tests caught and I fixed:
1. Crossing placed words can spell a puzzle word a second time (EEL out of ANEMONE + FLOUNDER). Fill can't repair that, so such layouts are now discarded and retried.
2. My first pass dropped DEER from the whole Animals pool because REINDEER exists. Wrong level — now handled per puzzle draw.

**Measured:** 100 hard puzzles, 18 words each, in 2.7 s on this machine (Node). Fine for the browser.

**Not done / carried:** push (blocked on credentials). Phase 2 (PDF) is next; plan entry follows.

## Phase 2 — PDF pipeline

Done. `app/src/pdf/`:

- `kdp.js` — six trim sizes, gutter-by-page-count table, bleed geometry (page grows 0.125" wide / 0.25" tall, outer margins 0.375"), inside/outside margins swap by page parity.
- `render.js` — pdf-lib. Title page, copyright page, one puzzle per page (grid + word bank in 1–4 columns sized to the trim), "Solutions" divider forced onto a right-hand page, solutions 6-up on tall trims / 4-up on 5×8, Notes pages to reach 24 and an even count. Page number on the outside corner. Watermark footer unless `licensed: true`. Liberation Sans (SIL OFL) embedded and subset — `pdffonts` confirms `emb yes`.
- Tests: 7 new (18 total, all pass). KDP geometry numbers checked exactly. Two sample books written to `app/samples/` (6×9 free-tier, 8.5×11 bleed licensed), rasterized with `pdftoppm`, and looked at: grids, banks, shaded solutions and footers all where they should be.

**Changed from plan:** solutions default was 4-up; the 6×9 page had half its height empty, so it now packs 6 per page where a grid stays ≥ 2.1" — fewer pages, lower print cost for the seller. Also removed a domain name I had put in the watermark; I don't own one.

**Verified, not merely claimed:** page sizes 432×648 / 621×810 pt, page counts match `planPages`, fonts embedded, title metadata set.

**Not verified:** actual KDP upload. I have no KDP account and will not open one. The spec numbers come from KDP's published help page; a real upload is the only final test, and that is the boss's account or a customer's.

## Phase 3 — UI + free tier

Done. `app/public/index.html`, `app/src/ui/main.js`, `app/src/ui/license.js`, bundled by esbuild (`npm run build` → `public/app.js`, 1.3 MB raw / 560 KB gzipped; pdf-lib + fontkit are most of it).

- Settings on the left: title/subtitle/author, trim, count, bleed, themes (multi-select) and/or pasted list with a name, words per puzzle, difficulty, grid size (auto), seed + Reshuffle. Live HTML preview of the first 3 puzzles on the right. Page-count estimate. Warnings surfaced (dropped words, unfit words).
- Free tier: download capped at 5 puzzles with the watermark; the tier box says so and links to an Unlock dialog. The dialog has a Buy link only if the Worker injects `PUZZLE_PRESS_PAY_URL` (phase 5), and an "email used at checkout" field that calls `/api/verify` (phase 5). Licence is a localStorage record.
- Public sample book regenerated by `npm run sample` → `public/samples/sample-6x9.pdf` (20 puzzles, Garden & Kitchen, licensed, no watermark). Linked from the header.

**Tested:** `test/browser.mjs` drives real Chromium (Playwright) against the built site: loads page, free-tier download → 24-page PDF with 5 puzzles; fakes a licence, switches to 8.5×11 hard with a pasted "Fruit Bowl" list, 60 puzzles → 74-page PDF, no warnings, no console errors; 400px viewport has no horizontal overflow. Screenshots in `app/samples/browser/` (ignored). Desktop screenshot reviewed by me.

**Changed from plan:** test PDFs moved to an ignored `samples/test/` so test runs don't churn git; the public sample lives in `public/samples/`. Firefox on this machine is a snap stub and can't run headless, so I installed Playwright's Chromium (dev dependency, ~100 MB, local only).

**Not done:** nothing from the phase 3 plan. Push still blocked on credentials.

## Phase 4 — deploy

Done. Live on two URLs, both tested by me in a real browser:

- **https://puzzlepress.bananafest-destiny.com** ← the one to share
- https://puzzle-press.walkertbrown.workers.dev (kept as a fallback)

- `app/src/worker.js` — static assets, `GET /config.js` injecting `PAY_URL`, `POST /api/verify` (Stripe check, inert until the key exists). `app/wrangler.jsonc` — assets from `public/`, `run_worker_first` for `/config.js` and `/api/*`, custom domain route, `workers_dev: true`.
- Deployed with the boss's Cloudflare token. Version `e5631f2d`. 6 assets, Worker startup 5 ms.
- **Verified live:** `/`, `/app.js`, `/config.js`, `/fonts/*.ttf`, `/samples/sample-6x9.pdf` all 200 with correct content types; `/nope` → 404; `/api/verify` → 405 on GET, 503 with a plain-English message on POST. Full Playwright run against the custom domain: free-tier download → 24-page PDF, licensed 60-puzzle 8.5×11 → 74 pages, unlock dialog correct, no console errors, no overflow at 400px. TLS verifies (`ssl_verify_result 0`).

**Changed from plan:** plan said workers.dev only. The boss then gave me the studio domain, so the product sits on `puzzlepress.bananafest-destiny.com` and workers.dev stays enabled as a fallback. Adding a route silently disables workers.dev, so `workers_dev: true` is explicit in the config.

**Snags, both mine to note not to fix:** immediately after each deploy, asset paths 404 for ~a minute before propagating — not a bug, just don't test instantly. And this machine's resolver had cached NXDOMAIN for the new subdomain; public resolvers (1.1.1.1, 8.8.8.8) were correct within seconds. `test/browser.mjs` gained a `HOST_MAP` env var to pin DNS for a run so a stale local cache can't block a live test.

**Still blocked:** `git push` → `remote: Permission to walkertbrown/vibe-cider.git denied to walkertbrown` (403). The GitHub line is back in `.git-credentials` and authenticates, but the PAT has no write on this repo. 10 commits local.

## Phase 6 — marketing

Done, and live at https://puzzlepress.bananafest-destiny.com (version `fa83d004`).

**The page now sells.** It opens with a hero (what it makes, who for, free CTA + the sample book), then the tool, then a demo, then four reasons the manuscript won't be rejected, four reasons the puzzles hold up, a pricing block with a visible Buy button, and seven FAQs. Footer credits Bananafest Destiny.

**Findable.** Title/description written for "word search book generator KDP". Canonical, Open Graph and Twitter card tags. JSON-LD for `SoftwareApplication` (with the $19 offer) and `FAQPage`. `robots.txt` and `sitemap.xml`. The FAQ answers are the actual SEO play — real answers about gutter margins, trim sizes and bleed, on the page that holds the tool.

**Artifacts that travel**, all rendered from the real product, none mocked up:
- `social-card.png` — 1200×630 card for link previews, built from a real puzzle page.
- `hero-book.png` — a puzzle page beside its solutions page.
- `demo.gif` — 283 KB, 7 frames: title the book, switch to the Christmas theme, set 60 puzzles on hard, download, then two pages of the PDF it just made. `npm run demo` re-records it against a live or local server.
- `npm run images` and `npm run demo` regenerate all three, so they never go stale against the product.

**Changed from plan:** nothing dropped, one addition — the demo GIF wasn't in the plan as a deliverable, only "a short demo". There's no ffmpeg on this machine, so I encoded the GIF directly with `gifenc` + `pngjs` (dev dependencies) from Playwright frames.

**Verified:** all four new assets return 200 with correct sizes; page is 23 KB; no console errors; no horizontal overflow at 400 px; the full Playwright e2e still passes against live (free-tier 24-page book, licensed 60-puzzle 74-page book, unlock dialog showing the live Buy link).

**Not done — and it needs the boss.** Everything above makes the product persuasive to someone who arrives. None of it makes anyone arrive. Distribution asks are in the conversation.

## Phase 7 — pre-launch value

Done and deployed (version `f85e2e70`).

- **Gallery slots 4 and 5** rendered from the real app and a real book: `public/gallery/04-the-tool.png` (the tool mid-use, 100-puzzle large-print book being set up) and `05-solutions.png` (a solutions page, close). `npm run gallery` regenerates them. Launch file updated with the paths.
- **Themes: 10 → 32.** Added halloween, thanksgiving, easter, valentines, birthday, wedding, states, farm, dinosaurs, camping, fishing, cars, weather, jobs, school, baby, coffeetea, birds, flowers, desserts, boardgames, beach. Chosen for what KDP sellers actually publish — seasonal and giftable, not generic. The existing test "every built-in theme generates a clean 20-puzzle hard book" now covers all 32 and passes, so every new list is proven to make solvable books.
- **Large print preset.** One checkbox sets 8.5×11, a 15×15 grid and 14 words — ~23pt letters. It was always possible and nobody would have found it. Changing trim, grid or word count by hand clears the checkbox, so the label never lies about what is set.
- **Word bank now scales with the page.** Caught by looking at the first large-print page I generated: 23pt grid letters above a 13pt word bank reads as fine print in a book sold on being readable. Bank size now derives from page width (10–17pt).
- Page says "32 themes" and "Large print option" in the hero and trust row.
- Regenerated the sample book, hero, social card, gallery and demo GIF so every published asset shows current output.

**Changed from plan:** the word bank scaling wasn't planned — it was a defect the large-print work exposed. Fixed rather than shipped.

**Verified:** 18 unit tests pass; live e2e passes (free 24-page, licensed 60-puzzle 74-page); large-print path checked end to end in a browser and the PDF page inspected by eye; all five gallery/marketing assets return 200 live.

**Left for Monday:** re-run the purchase test against live before the launch window.

## Phase 8 — cover generator

Done and deployed (version `561620d9`). This was my call on what to build next, and it is the gap between "an interior PDF" and "a book you can publish".

`app/src/pdf/cover.js` produces a full wrap — back cover, spine, front cover — on one PDF page, sized from the page count of the interior it just made.

**Getting the spine right was the whole job.** Two well-ranked third-party KDP calculators state the paperback formula as `(pages × thickness) + 0.06"`. KDP's own "Create a Paperback Cover" page says the opposite: spine width is page count times paper thickness, with **nothing added** — the 0.06" is a hardcover rule. I checked the source rather than the aggregators, and every cover would have been 0.06" out otherwise. There is a test asserting the wrong figure is not used.

- Per-page thickness from KDP: white 0.002252", cream 0.0025", premium colour 0.002347", standard colour 0.002252". Paper is now a setting, because it changes the spine.
- Cover width = bleed + back + spine + front + bleed; height = bleed + trim + bleed; bleed 0.125".
- Spine text only from 79 pages, per KDP. Below that the spine is left blank automatically.
- The 2" × 1.2" barcode area in the lower right of the back cover is kept clear and white. KDP's help page doesn't give that number; it is the convention their own downloadable templates use, and the code says so.
- Design is generated, not stock: a faint letter field across the wrap (kept off the spine), a title block, and a real puzzle from the book printed small on the back under "A puzzle from inside".
- The page count and exact cover size, including spine, are shown live under the buttons so a seller can check them against KDP's own calculator.

**Cover generation is the paid unlock.** The free tier still makes a real interior; a finished book needs the wrap. Clicking "Download cover" without a licence opens the unlock dialog. That gives the $19 a concrete thing to be for.

**Verified:** 6 new tests (24 total, all pass) including the geometry equations and the anti-`+0.06` guard; browser run confirms free tier is gated, licensed download produces a 1-page 12.550" × 9.250" PDF for a 120-page cream book (spine 0.300"), fonts embedded, no console errors; the rendered cover rasterized and inspected by eye.

Landing page, pricing block and FAQ updated to say the cover exists and that it is part of the paid unlock.

## Phase 8b — copy caught up with the product

The cover changed what this is, so everything describing it had to change too. Found by re-reading the page after shipping, not by anyone reporting it.

- **"How it works" was actively wrong.** Step 3 read "Upload it as your paperback interior. Add a cover, publish." — telling people to go and make the cover the app now makes. Rewritten to three steps ending "Upload both to KDP and publish."
- **Product Hunt copy rewritten.** Description now leads with interior *and* cover (244/260). The first comment gains a paragraph on why the cover was the hard part — the spine can't be sized until the interior exists, and the two top-ranked spine calculators online add 0.06" that Amazon's own docs don't. That is a genuinely useful warning for the audience and it is true. Large print added. The free/paid line now says the unlock includes cover generation. Likely-question answers updated.
- **Demo GIF re-recorded** — 9 frames now, ending on the cover it just generated. Runs licensed so the paid step is visible.
- **Social card** copy updated to "Interior and cover, print-ready."

**Verified:** deployed as `97533454`, waited for edge propagation and confirmed the live HTML, card and GIF are the new ones by size and content; live e2e still passes.

Worth noting for next time: the launch reminder routine reads `app/marketing/product-hunt.md` at fire time rather than carrying a copy of the text. That decision is why tonight's rewrite reaches Monday's email automatically.

## Phase 8c — the numbers on screen now describe the file you get

Deployed as `3f1845a7`.

**A real defect, found by auditing rather than by a report.** The preview quoted page count, cover size and spine width for the number of puzzles you *asked for*, not the number you would actually receive. On the free tier that meant asking for 50 puzzles showed "62 pages, spine 0.155"" while the file you downloaded was 24 pages with a 0.060" spine. Anyone trusting those numbers to order a cover would have got it wrong by nearly a hundredth of an inch per page. Every figure is now computed from the effective count, and the line says so plainly: "5 puzzles · 6" × 9" · 24 pages · free tier, 50 once unlocked". Unlocking re-runs the preview immediately so the numbers change the moment the cap lifts.

- **Sample cover published.** `samples/sample-cover-6x9.pdf`, linked in the header and the hero, generated by `npm run sample` alongside the sample book. The cover is the paid half, so a visitor should be able to judge it before paying rather than after.
- Sitemap updated.
- Added a temporary audit script that downloads the interior and the cover and asserts the PDF page count matches what the page claimed, and that the cover width matches the quoted spine. Both matched on free and paid paths. Removed after use; the assertions worth keeping are already covered by the unit tests.

**Correction to something I said earlier:** I described the sample book as predating the word-bank scaling fix. It does not — `git log` shows the sample and `render.js` were committed together in `fdb69b5`. The sample was current; what it lacked was a cover, which is now published.

**Verified:** 24 unit tests pass; audit passes on both tiers; no overflow at 400px with the new paper and cover controls; live e2e passes; sample cover returns 200.

## Phase 8d — auditing for disagreements

Two passes, two findings. Deployed as `8e09ab17`.

**1. The invariant that protects every cover was untested.** The cover's spine width comes from `planPages()`; the interior comes from `renderBook()`. If those ever disagree about how long a book is, every cover this app produces is the wrong size — and nothing would have caught it. `test/invariants.test.js` now renders a real book for every trim × count × bleed combination (84 books) and asserts the rendered page count equals the planned one. It holds. Also pinned: minimum 24 pages and even count for every trim, and cover width = back + spine + front + two bleeds.

**2. A claim on the landing page was false.** It said "A 100-puzzle book is 100 different puzzles, not a shuffle of the same twenty." Paste 8 words, ask for 8 per puzzle and 20 puzzles, and you got **20 puzzles with an identical word list** and no warning at all. The maths is unforgiving: a pool of 8 taken 8 at a time has exactly one possible list.

Fixed at the source rather than by softening the copy. `generateBook` now computes C(pool, take) — capped so a big pool never overflows — and warns when it is smaller than the book needs:

> Fruit: 8 words taken 8 at a time makes only 1 different word list, but this book needs 3. Some puzzles will repeat the same words — add more words, or lower words per puzzle.

The page copy now also says the tool tells you, which is a better claim than the one it replaced because it is true and it is checkable.

Also tested and confirmed while I was in there: the FAQ's promise that the same seed and settings rebuild the identical book.

**Verified:** 34 tests pass (was 24). Warning confirmed in the live UI with a real 8-word paste. No console errors.

## Phase 8e — the rest of the page, checked against the code

Deployed as `a35f1d24`.

Walked every remaining factual claim on the landing page and checked it against what the code does:

- **Gutter margins** — FAQ says 0.375" to 150 pages, 0.5" to 300, 0.625" to 500, 0.75" to 700, 0.875" beyond. `gutterInches()` returns exactly that at every boundary. Correct.
- **Outside margins** — FAQ says 0.25", or 0.375" with bleed. The geometry measures from the PDF page edge, so with bleed it reports 0.5" = 0.375" from the trim line plus the 0.125" bleed. Correct, and consistent with what KDP asks for.
- **Six trim sizes, 24-page minimum** — both match.
- **"Nothing you type ever leaves your browser"** — proven, not asserted. `test/privacy.mjs` types a nonsense token into the title, author, list name and word list, generates a book and a cover, and records every network request. Result against the live site: two requests, both for the embedded fonts. Zero off-site requests. Zero requests containing the typed token. The unlock check sends exactly `{"email":"…"}` and no other field. Kept as `npm run test:privacy`, because this is precisely the claim that breaks silently the day someone adds an analytics snippet or a CDN font.

**Added: terms in plain English**, anchored at `#terms` and linked from the footer. Four short sections: the books are yours (no copyright claimed, no royalty), what $19 buys, what is stored (nothing you type), and what is not promised — that KDP can change its rules or reject a file for its own reasons, and that the numbers are shown on screen so a seller can check them against KDP's own calculator before printing. A paid product being launched publicly should say these things out loud, and the "can I sell what I make" answer is the first question every KDP tool gets.

**Verified:** 34 unit tests, privacy suite and the live e2e all pass against production.

## Open ask for the boss

A buyer with a problem currently has no way to reach anyone. I will not invent a support address or hand out an email that was not offered. If there is an address that should go on the page — a support alias, or a personal one you are happy to publish — say so and I will add it to the terms section. If you would rather have none, that is a fine answer too and I will leave it as it is.

## Phase 8f — support address, and a stale launch asset

**Support address: not published yet, deliberately.** The boss offered `support@bananafest-destiny.com`. Before putting it in front of buyers I checked whether it can actually receive mail: the zone's MX records point at `fwd1/fwd2.porkbun.com` with a Porkbun SPF record, so forwarding is handled at the registrar, not in Cloudflare, and nothing I can read says whether the `support` alias exists. I sent one test message to it from the boss's own Gmail; several minutes later only the SENT copy exists and nothing has arrived in the inbox. So the alias is not forwarding yet. Asked the boss to add it in Porkbun (Domain Management → Email Forwarding → `support` → their Gmail); I will re-run the test and only then add it to the terms section. A support address that silently swallows a buyer's email is worse than no address at all.

**Gallery slot 4 was stale and I nearly launched with it.** It was shot in phase 7, before the cover existed. It showed no paper selector, no Download cover button, and 10 fewer themes — a screenshot of a product that no longer exists, sitting in the launch file as the thing that shows people what they get. Regenerated, and two problems with the regeneration were worth fixing rather than shipping:

1. The script ran unlicensed, so the shot read "5 puzzles · 24 pages · free tier" underneath a title promising 100 puzzles. Honest, but in a promotional image it reads as a bug. It now shoots the paid product.
2. Framing on the viewport either cropped the download buttons or cropped the title. It now screenshots the `main` element directly, so settings and preview arrive together at natural height — 1920×2399, everything in one frame: title, paper, all 32 themes, both download buttons, the live cover size, and the preview reading "100 puzzles · 6" × 9" · 120 pages".

**Lesson worth writing down:** generated assets need regenerating whenever the thing they depict changes, and there is currently nothing that tells me when that is. `npm run images`, `npm run gallery`, `npm run demo` and `npm run sample` all exist precisely so this is cheap — the failure was not running them, not the tooling.

## Phase 9 — sudoku

Shipped and live (version `a5c5894d`). Puzzle Press now makes two kinds of book.

`src/generator/sudoku.js`: a complete grid by randomised backtracking, then clues dug out in 180-degree symmetric pairs — the convention in printed books — keeping a removal only when the grid still has exactly one solution. Uniqueness is the entire job: a sudoku with two answers makes the solutions page at the back of the customer's own book wrong, and that is the kind of error a reviewer names.

**Two things the tests caught before anyone else could.**

1. *Expert did not do what it said.* The first version dug once over one grid and accepted whatever it reached — expert asked for 24 clues and delivered 30. Some complete grids simply will not dig that deep. Now it digs in several passes and, if a grid will not reach the band, starts from a different complete grid.
2. *And then expert was too slow to ship.* Measuring the cost per clue found a cliff: 28 clues costs ~14 ms a puzzle, 27 costs ~94 ms, 26 costs ~264 ms, 24 over a second. At 24, a 100-puzzle book would have locked the tab for nearly two minutes. Expert is set at **26** — what published expert puzzles actually carry, and the honest floor for symmetric digging rather than an arbitrary one. The reasoning is a comment in the source, with the numbers.

Because even 26 is real work, book generation for sudoku is async and yields between puzzles, reporting "Generating puzzle 37 of 60…". A frozen tab reads as a crash.

Rendering: box borders are drawn markedly heavier than cell borders — at the first attempt they were nearly equal and the 3×3 structure vanished, which makes a puzzle unpleasant to solve. Grids are centred in the page rather than hung off the header. On the solutions pages the original clues stay bold black and the filled-in answers are grey, so a solver can see what they were given. Covers handle sudoku too, with a miniature grid on the back and a blurb that suits the puzzle type.

**Verified:** 42 tests pass (was 34), 8 of them sudoku-specific — uniqueness at every difficulty, the stated solution matching the solver, the puzzle being the solution with cells removed and never altered, 180-degree symmetry, difficulty ordering and exact clue counts, determinism, and a 40-puzzle book with no repeats. Driven end to end in a real browser: type switch hides the word controls and restores them, preview shows 32 clues for medium, a 60-puzzle book renders 74 pages, cover downloads. Pages rasterised and inspected by eye. Live e2e and privacy suites still pass.

Copy updated everywhere it needed to be: title, meta description, header, hero, trust row, a new "every sudoku has exactly one answer" section, a new FAQ, and the Product Hunt file — where the answer to "does it do sudoku?" had been "not yet".

## Phase 9b — launch assets catch up with sudoku

Deployed as `673cd356`.

Sudoku shipped, which made every generated asset a picture of a product that no longer matched. Rather than wait to be told, regenerated all of them:

- **Demo GIF** — 12 frames now. First attempt was incoherent: I dropped the sudoku switch between the interior download and the cover download, so the GIF showed a word-search interior followed by a cover titled "Sudoku for Sunday". Re-sequenced so each type produces its own output — word search book, its cover, then a switch to sudoku and a sudoku book — and the closing frames show real pages from each, including "Sudoku too — one answer each".
- **Gallery slot 6** added: a hard sudoku page beside its solutions page, with the one-solution guarantee as the caption. That is the newest thing in the product and the thing a browser of the gallery will not expect.
- **Sudoku sample book published** — `samples/sample-sudoku-6x9.pdf`, 20 medium puzzles, linked in the header next to the word search sample and the sample cover. The samples do the selling; there should be one for each kind of book.
- Sitemap and the Product Hunt gallery list updated.

**Verified:** all three new assets return 200 live; the demo GIF served matches the new build byte for byte; live e2e passes.

## Phase 10 — mazes

Shipped and live (version `ad413fbe`). Three puzzle types now.

`src/generator/maze.js`: iterative randomised depth-first search — iterative because a 39×39 maze is 1521 cells deep in the worst case and recursion there is asking for trouble. Every maze is a spanning tree, so "every cell reachable, no loops, exactly one route from start to finish" is true by construction, not by inspection. The route comes from a breadth-first search, which in a tree is the only path as well as the shortest.

Performance never became a problem: expert is 6 ms a maze, and merging wall runs before drawing turns a 39×39 from roughly 3,000 line segments into 719 — a smaller PDF and a faster render. A 60-maze hard book is 693 KB.

**One real defect, found by looking at the output.** The first solutions page drew the route as a hairline in the same pale grey used for shading. At six mazes to a page it was invisible — an answer key that cannot be read is worse than no answer key, because the buyer only discovers it after printing. The route is now a wide grey corridor drawn under the walls, and it reads clearly at six-up.

Entrance and exit are holes in the border, deliberately kept out of the cell graph so the maze stays a clean spanning tree and `isPerfect` still means what it says; they exist only when walls are drawn. Pages are labelled "start" and "end" outside the walls.

**Verified:** 49 tests pass (was 42), 7 of them maze-specific — perfection at every difficulty, reciprocal openings (no one-way walls), the solution being a genuine adjacent-cell walk that never passes through a wall, size and route length increasing with difficulty, wall runs actually merging, the border actually being opened, determinism, and a 30-maze book with no repeats. Driven in a real browser across all three puzzle types: previews render, difficulty lists swap, a 60-maze book comes out at 74 pages, cover downloads, no overflow at 400 px, no console errors. Pages rasterised and inspected. Live e2e and privacy suites pass.

Copy updated: title, meta description, header, hero, trust row, a new "every maze has exactly one route" section, the puzzle-types FAQ, and the Product Hunt file.

## Phase 10b — assets and samples for three puzzle types

Deployed as `8eb0aed3`.

- **Maze sample book published** — `samples/sample-maze-6x9.pdf`, linked in the header. There is now a sample of every kind of book the tool makes, plus a sample cover. These are what a visitor clicks before deciding, so each one has to exist.
- **Demo GIF re-recorded** — 15 frames, now walking word search → its cover → sudoku → mazes, closing on real pages from each including "And mazes — one route through".
- **Gallery slot 7** added: a hard maze page beside its six-up solutions page.
- Sitemap and the Product Hunt gallery list updated again.

**One thing I fixed rather than shipped:** the GIF came out at 626 KB, which is too much to hang on a landing page. The app is flat colour and the PDF frames are near-monochrome, so the 256-colour palette was paying for nothing. Dropping to 64 colours took it to 525 KB with no visible difference. It is lazy-loaded and below the fold, which is the other half of that being acceptable.

**Verified:** deployed, waited out the edge (first poll returned 404s for the new files — normal, and the reason I poll rather than trust the deploy), then confirmed the maze sample, gallery slot 7 and the new GIF byte length are all live. Live e2e passes.

## Phase 11 — cover previews and a spine calculator

Deployed as `9f799379`.

**Free users can now make their own cover.** It carries their title, their subtitle, their author name and a spine sized for the book they would actually get, with a PREVIEW mark across the front and a line under it saying what removes it. The unlock dialog opens straight after. Asking someone to pay for a cover they have never seen, on the evidence of a sample of someone else's book, was leaving money on the table.

The mark needed one fix: sized from panel height it overflowed the front cover onto the back. It is now sized so the rotated word fits inside the front panel.

**A free KDP spine width calculator** at `/spine-calculator`: trim, page count and paper in; spine width, full cover size, inside margin, spine-text eligibility and the barcode reserve out, in inches and millimetres, with the arithmetic shown.

It exists because of something I found while building the cover: several of the highest-ranked KDP spine calculators tell you to add 0.06" that Amazon's own documentation does not — that figure is a hardcover rule. The page says so plainly. Being the correct one is the reason to link to it, and the people searching for it are building a KDP paperback at that moment.

**Two things worth recording:**

1. The calculator first built to **1.3 MB**, because it imported cover geometry from a module that also pulls in pdf-lib and fontkit. Pure geometry now lives in `src/pdf/cover-geometry.js` with no heavy imports; the renderer re-exports from it, so both still speak from one source and cannot drift. The page is now **2.5 kB**.
2. My own test expectation was wrong before the code was: I asserted 8.5×11 at 200 pages gives a 17.500" cover. It is 0.125 + 8.5 + 0.5 + 8.5 + 0.125 = 17.750", which is what the calculator returned. Fixed the test, left a comment saying which of us was wrong.

Also found: Cloudflare serves assets at extensionless URLs and 307s the `.html` form, so every link, the canonical tag and the sitemap now use `/spine-calculator`.

**Verified:** 49 unit tests pass. Two new live suites kept as scripts — `npm run test:spine` checks three known-correct cases against production and the 79-page spine-text rule; `npm run test:freecover` checks a free cover carries the buyer's own title, opens the unlock dialog and is larger than the paid file, while a paid cover does neither. No overflow at 400 px, no console errors.

## Phase 12 — money, shown while you build

Deployed as `e9d73978`. 58 unit tests (was 49).

**A KDP royalty calculator** at `/royalty-calculator`, and — more useful — the same figures live inside the tool: *"Prints for $2.44 on Amazon.com — at $9.99 you keep $3.55 a copy."* Sellers care about margin more than they care about anything else on that page, and it now updates as they add puzzles.

**Getting the rates right took two passes at Amazon's own documentation.** The first fetch gave me the regular-trim table. But this app offers 8.5×11, 8×10 and 7×10, which are *large trim* — more than 6.12" wide or more than 9" tall — and those print at different rates: $0.017 a page instead of $0.012, and a $2.84 flat rate under 110 pages instead of $2.30. Quoting regular-trim costs for a large-trim book would have been exactly the error I put a warning box on the spine page about. 6×9 is regular trim, because exactly 9" is not "more than" 9".

This produced a genuinely useful piece of copy for the royalty page: going from 6×9 to 8.5×11 for bigger grids raises the per-page cost by about 40%, which is worth it for a large-print book people pay more for and expensive by accident otherwise. That is the kind of thing the tool can now tell someone at the moment they tick the box.

`src/pdf/kdp-cost.js` is pure arithmetic with no heavy imports, so the calculator page is 3.7 kB and the tool, the page and the tests all quote one source.

**Verified against production:** KDP's own worked example ($16.99, 300pp, 6×9 → prints $4.60, keeps $5.59) matches to the cent, plus three more cases across both trim classes; a below-cost price warns instead of quietly showing a negative; an unprintable ink/page combination explains itself; and the tool's own money line was checked against the rate table for the page count it reported. Kept as `npm run test:royalty`.

## Phase 13 — reading the page cold

Deployed as `4e39dc4f`. I pulled the live page down as plain text and read all 163 lines of it as someone arriving for the first time. The product has moved four times since the copy was first written and the copy had not kept up. Nine things wrong, none of which any test would ever catch:

**It still talked like a word-search tool in the places that matter most.**
- The hero promised "Pick from 32 themes or paste your own words" and "puzzles, word banks, solutions" — none of which is true for two of the three puzzle types. Someone arriving for sudoku would read the headline as not-for-them.
- "How it works" step 1 told every visitor to "Pick themes or paste your own words", including the ones making mazes.
- The quality section opened "Anyone can scatter letters in a grid" and then covered mazes and sudoku.
- Three word-search-only guarantees sat under type-neutral headings, next to the maze and sudoku ones, so none of them said who they applied to. They are now prefixed "Word search:", "Sudoku:", "Mazes:" and "Every book:".
- "Does my word list get uploaded anywhere?" — a privacy question phrased as if only word searches had inputs.
- "How many puzzles should a book have?" answered only for word search books.
- The trust row claimed "32 themes, or your own words" for all three types.

**The most important question was buried.** "What kinds of puzzles can it make?" sat seventh in the FAQ, below margins, trim and bleed. For a first-time visitor that is the question, and it is now first.

**The page was written in three voices** — "I claim no rights" in one place, "we claim no copyright" in another, "Tell us" in a third. Now consistently "we", because the footer says this is a studio.

**The structured data had gone stale separately.** The JSON-LD FAQPage still listed the original five questions, including ones no longer on the page, and none of the new ones. Invisible to a reader, which is exactly why it rotted. Rewritten to match what is actually there, seven questions, and checked that it parses.

**Verified:** live text re-read after deploying; all four live suites pass against production — browser end-to-end, royalty, spine, privacy.

The lesson is the same one as the stale screenshot, and it keeps costing me: the product changes and everything describing it silently becomes a lie. Tests catch code drift. Only reading the thing catches copy drift.
