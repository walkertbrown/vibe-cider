# Actual — 2026-09-10

## Phase 0 — commit to the app

Done. Puzzle Press chosen, written to `FACTS.md`. Plan written. Boss answered four factual questions (Cloudflare token on request, GitHub credentials coming, Stripe exists, Reddit account is new and subs disallow advertising, Cloudflare free plan). All in `FACTS.md`; inferences in `LEARNED.md`.

**Deviation:** `git push` fails — `.git-credentials` is not on this machine. Commits are local until the boss provides it. Asked in the thread.

## Phase 1 — generator core

Done. `app/src/generator/`:

- `rng.js` — seeded PRNG (mulberry32 + cyrb53). A book regenerates identically from its seed.
- `wordsearch.js` — placement in up to 8 directions, three difficulty levels (easy: E/S; medium: +diagonals; hard: all 8 + filler letters drawn from the puzzle's own words), overlap allowed on matching letters, `suggestSize()` for grid size from the word list.
- `blocklist.js` — strings the filler must never spell. Checked only against filler cells, so a user's GRASS or SHELL is not punished.
- `wordlists.js` — 10 themes, 47–76 words each.
- `book.js` — N puzzles from one or more pools, each puzzle a distinct word subset, nested pairs (DEER/REINDEER) kept apart per puzzle instead of dropped from the pool.
- `test/wordsearch.test.js` — 11 tests, all pass. Includes: every word exactly once; blocked strings never via filler across 40 puzzles; every built-in theme makes a clean 20-puzzle hard book.

**What changed from plan:** nothing in scope. Two bugs the tests caught and I fixed:
1. Crossing placed words can spell a puzzle word a second time (EEL out of ANEMONE + FLOUNDER). Fill can't repair that, so such layouts are now discarded and retried.
2. My first pass dropped DEER from the whole Animals pool because REINDEER exists. Wrong level — now handled per puzzle draw.

**Measured:** 100 hard puzzles, 18 words each, in 2.7 s on this machine (Node). Fine for the browser.

**Not done / carried:** push (blocked on credentials). Phase 2 (PDF) is next; plan entry follows.

## Phase 2 — PDF pipeline

Done. `app/src/pdf/`:

- `kdp.js` — six trim sizes, gutter-by-page-count table, bleed geometry (page grows 0.125" wide / 0.25" tall, outer margins 0.375"), inside/outside margins swap by page parity.
- `render.js` — pdf-lib. Title page, copyright page, one puzzle per page (grid + word bank in 1–4 columns sized to the trim), "Solutions" divider forced onto a right-hand page, solutions 6-up on tall trims / 4-up on 5×8, Notes pages to reach 24 and an even count. Page number on the outside corner. Watermark footer unless `licensed: true`. Liberation Sans (SIL OFL) embedded and subset — `pdffonts` confirms `emb yes`.
- Tests: 7 new (18 total, all pass). KDP geometry numbers checked exactly. Two sample books written to `app/samples/` (6×9 free-tier, 8.5×11 bleed licensed), rasterized with `pdftoppm`, and looked at: grids, banks, shaded solutions and footers all where they should be.

**Changed from plan:** solutions default was 4-up; the 6×9 page had half its height empty, so it now packs 6 per page where a grid stays ≥ 2.1" — fewer pages, lower print cost for the seller. Also removed a domain name I had put in the watermark; I don't own one.

**Verified, not merely claimed:** page sizes 432×648 / 621×810 pt, page counts match `planPages`, fonts embedded, title metadata set.

**Not verified:** actual KDP upload. I have no KDP account and will not open one. The spec numbers come from KDP's published help page; a real upload is the only final test, and that is the boss's account or a customer's.

## Phase 3 — UI + free tier

Done. `app/public/index.html`, `app/src/ui/main.js`, `app/src/ui/license.js`, bundled by esbuild (`npm run build` → `public/app.js`, 1.3 MB raw / 560 KB gzipped; pdf-lib + fontkit are most of it).

- Settings on the left: title/subtitle/author, trim, count, bleed, themes (multi-select) and/or pasted list with a name, words per puzzle, difficulty, grid size (auto), seed + Reshuffle. Live HTML preview of the first 3 puzzles on the right. Page-count estimate. Warnings surfaced (dropped words, unfit words).
- Free tier: download capped at 5 puzzles with the watermark; the tier box says so and links to an Unlock dialog. The dialog has a Buy link only if the Worker injects `PUZZLE_PRESS_PAY_URL` (phase 5), and an "email used at checkout" field that calls `/api/verify` (phase 5). Licence is a localStorage record.
- Public sample book regenerated by `npm run sample` → `public/samples/sample-6x9.pdf` (20 puzzles, Garden & Kitchen, licensed, no watermark). Linked from the header.

**Tested:** `test/browser.mjs` drives real Chromium (Playwright) against the built site: loads page, free-tier download → 24-page PDF with 5 puzzles; fakes a licence, switches to 8.5×11 hard with a pasted "Fruit Bowl" list, 60 puzzles → 74-page PDF, no warnings, no console errors; 400px viewport has no horizontal overflow. Screenshots in `app/samples/browser/` (ignored). Desktop screenshot reviewed by me.

**Changed from plan:** test PDFs moved to an ignored `samples/test/` so test runs don't churn git; the public sample lives in `public/samples/`. Firefox on this machine is a snap stub and can't run headless, so I installed Playwright's Chromium (dev dependency, ~100 MB, local only).

**Not done:** nothing from the phase 3 plan. Push still blocked on credentials.

## Phase 4 — deploy

Done. Live on two URLs, both tested by me in a real browser:

- **https://puzzlepress.bananafest-destiny.com** ← the one to share
- https://puzzle-press.walkertbrown.workers.dev (kept as a fallback)

- `app/src/worker.js` — static assets, `GET /config.js` injecting `PAY_URL`, `POST /api/verify` (Stripe check, inert until the key exists). `app/wrangler.jsonc` — assets from `public/`, `run_worker_first` for `/config.js` and `/api/*`, custom domain route, `workers_dev: true`.
- Deployed with the boss's Cloudflare token. Version `e5631f2d`. 6 assets, Worker startup 5 ms.
- **Verified live:** `/`, `/app.js`, `/config.js`, `/fonts/*.ttf`, `/samples/sample-6x9.pdf` all 200 with correct content types; `/nope` → 404; `/api/verify` → 405 on GET, 503 with a plain-English message on POST. Full Playwright run against the custom domain: free-tier download → 24-page PDF, licensed 60-puzzle 8.5×11 → 74 pages, unlock dialog correct, no console errors, no overflow at 400px. TLS verifies (`ssl_verify_result 0`).

**Changed from plan:** plan said workers.dev only. The boss then gave me the studio domain, so the product sits on `puzzlepress.bananafest-destiny.com` and workers.dev stays enabled as a fallback. Adding a route silently disables workers.dev, so `workers_dev: true` is explicit in the config.

**Snags, both mine to note not to fix:** immediately after each deploy, asset paths 404 for ~a minute before propagating — not a bug, just don't test instantly. And this machine's resolver had cached NXDOMAIN for the new subdomain; public resolvers (1.1.1.1, 8.8.8.8) were correct within seconds. `test/browser.mjs` gained a `HOST_MAP` env var to pin DNS for a run so a stale local cache can't block a live test.

**Still blocked:** `git push` → `remote: Permission to walkertbrown/vibe-cider.git denied to walkertbrown` (403). The GitHub line is back in `.git-credentials` and authenticates, but the PAT has no write on this repo. 10 commits local.
